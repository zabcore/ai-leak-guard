# V1.3 Release-Blocker Sweep (§10.1 – §10.12)

Each release blocker mapped to the test / gate that proves it. All green
as of M6. Verification only — no product behaviour changed. The committed
`SUBMIT` flag stays **OFF** in `main`.

| #          | Blocker                                        | Proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **§10.1**  | The user's draft is never lost                 | `submit-core.test.ts` › _return-to-edit → zero submits, draft intact, cancelled logged_; ×3 `submit-*-adapter.test.ts` › _…return-to-edit → zero clicks, draft intact_; self-test: `self-test-runner.test.ts` › _DRAFT_PRESENT: refuses to run over an existing draft_ + `self-test-integration.test.ts` › _never overwrites an existing draft…_                                                                                                                                                                                                        |
| **§10.2**  | One send intent → at most one submission       | `submit-core.test.ts` › _two intents during HELD → one submission…_ + _two intents during DECISION → one submission after proceed_ + `createIdempotentResume` (invoked at most once); ×3 › _rapid double-Enter → coalesced → … one click_                                                                                                                                                                                                                                                                                                               |
| **§10.3**  | No hold without an active watchdog             | `submit-core.test.ts` › _Watchdog-A (scan) expiry → FAILED_OPEN → proceed…_ (the automated scan phase — text read + detect + pending-file wait — is always wrapped by a watchdog; expiry proceeds and logs `unable-to-inspect`)                                                                                                                                                                                                                                                                                                                         |
| **§10.4**  | No exception leaves a send held                | `submit-core.test.ts` › _scan throws synchronously_ / _scan rejects asynchronously_ / _readComposerText throws_ (each → FAILED*OPEN → proceeds); \_a throw inside the decision UI lands on RETURNED_TO_EDIT, not a submit* (a decision-phase throw terminates on RETURNED_TO_EDIT, never held)                                                                                                                                                                                                                                                          |
| **§10.5**  | IME never intercepts (any site)                | ×3 `submit-*-adapter.test.ts` › _IME composing Enter (isComposing) → never intercepted_ + _IME confirm Enter (keyCode 229) → never intercepted_                                                                                                                                                                                                                                                                                                                                                                                                         |
| **§10.6**  | Never two modals in sequence for one send      | `submit-text-file-coordination.test.ts` › _sensitive text + sensitive file ⇒ exactly ONE combined modal_ + _text + file ⇒ ONE modal, merged heading + summed count_                                                                                                                                                                                                                                                                                                                                                                                     |
| **§10.7**  | Acknowledgement never persists to storage      | `submit-core.test.ts` › _RELEASE BLOCKER: chrome.storage.local.set never receives a fingerprint (even with logging on)_ (spies on `chrome.storage.local.set`; no fingerprint, no `'fingerprint'` key, no matched content in any write)                                                                                                                                                                                                                                                                                                                  |
| **§10.8**  | No outbound network                            | `npm run verify:no-network` — scans every `dist/` chunk for `fetch(` / `XMLHttpRequest` / `sendBeacon` / `WebSocket` / `EventSource` / remote `import(...)`. Vendored format parsers (PDF.js etc.) are allowlisted BY NAME and printed each run — they carry network code but are fed in-memory data only (never a URL; PDF.js runs with `disableAutoFetch`/`disableStream`), so the path is unreachable in our usage. `window.open`/`chrome.tabs.create` to the report page are user-initiated tab opens, not extension requests, and are not flagged. |
| **§10.9**  | No new permissions                             | `manifest-permissions.test.ts` › _permissions list is exactly ["storage"]…_ (`permissions === ['storage']`, `optional_permissions === []`, `optional_host_permissions === []`, host_permissions unchanged)                                                                                                                                                                                                                                                                                                                                              |
| **§10.10** | No coverage claim beyond the 3 supported sites | **Copy check — verified at M7** (store listing / marketing). The extension code makes no broader claim; the honest GAP list lives in `docs/REGRESSION_MATRIX.md` and each adapter header. Noted here; final sign-off is the M7 copy pass.                                                                                                                                                                                                                                                                                                               |
| **§10.11** | No auto-submit of flagged content (A-1)        | `submit-core.test.ts` › _with the user never choosing, advancing past every budget leaves the send HELD with zero submissions_ + _if a liveness guard is enabled, its ONLY outcome is RETURNED_TO_EDIT — never SUBMITTED_ + _a throw inside the decision UI lands on RETURNED_TO_EDIT, not a submit_ + _module default: no liveness guard at all_                                                                                                                                                                                                       |
| **§10.12** | No V1.2.1 paste regression (A-7)               | `tests/paste-richtext.test.ts` — the full V1.2.1 rich-text/EMR paste suite stays green and unweakened; HTML-only SSN/MRN/NPI still warn at paste time with boundary-preserving un-gluing (see `docs/REGRESSION_MATRIX.md` › Carry-forward)                                                                                                                                                                                                                                                                                                              |

## Gate command

```
npm run typecheck && npm run lint && npm run test && npm run build && npm run verify:sw && npm run verify:no-network
```

All must be clean for a release build. `verify:no-network` is the M6
addition; the rest predate it.
