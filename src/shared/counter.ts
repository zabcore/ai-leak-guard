// Local leak counter logic. Implemented in a later issue.
export function getCount(): number {
  return 0
}

export function incrementCount(_ruleId: string): void {
  // No-op until counter logic lands.
}
